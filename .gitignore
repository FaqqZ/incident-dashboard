node_modules/
dist/
.env
*.log
.DS_Store

# Vercel
.vercel

# server/data/ es el directorio de RUNTIME: en el host es el punto de montaje
# del disco persistente y en local se siembra solo al arrancar. No se versiona.
server/data/

# server/seed/ SÍ se versiona: son los Excel que viajan en la imagen y siembran
# el volumen la primera vez. Contienen incidentes reales de Seguridad Ciudadana,
# así que este repositorio tiene que ser PRIVADO.
