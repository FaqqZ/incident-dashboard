node_modules/
dist/
.env
server/data/tmp/
*.log
.DS_Store

# Vercel
.vercel

# OJO: server/data/incidentes.xlsx SÍ se versiona (es el dato semilla que
# necesita el deploy). Contiene incidentes reales de Seguridad Ciudadana, así
# que este repositorio tiene que ser PRIVADO.

# Respaldos que genera "Limpiar base de datos"
server/data/backups/
