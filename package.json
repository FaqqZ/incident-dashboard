{
  "name": "incident-dashboard",
  "private": true,
  "version": "1.0.0",
  "description": "Raíz solo para el deploy en Vercel: instala las deps del backend (que usa la función serverless de api/) y construye el cliente.",
  "scripts": {
    "build": "npm --prefix client install --no-audit --no-fund && npm --prefix client run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1",
    "xlsx": "^0.18.5"
  }
}
